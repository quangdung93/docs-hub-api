# Pipeline TỰ CHỨA — KHÔNG include isc/cicd-config (chưa có template go/).
# Khi ISC có template Go chính thức, cân nhắc chuyển sang include.
stages:
  - lint
  - test
  - build
  - docker

default:
  image: golang:1.25
  tags:
    - docker

variables:
  GOFLAGS: "-buildvcs=false"
  GOMODCACHE: "$CI_PROJECT_DIR/.cache/go-mod"
  DOCKER_IMAGE: "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA"

cache:
  key:
    files:
      - go.sum
  paths:
    - .cache/go-mod

# ------------------------------------------------------------------ lint
lint:
  stage: lint
  script:
    - test -z "$(gofmt -l .)" || (echo "❌ Code chưa gofmt"; gofmt -l .; exit 1)
    - go vet ./...
    - go run github.com/golangci/golangci-lint/cmd/golangci-lint@v1.61.0 run --timeout 5m

# ------------------------------------------------------------------ test unit
test:unit:
  stage: test
  script:
    - go test -race -short -coverprofile=coverage.out ./...
    - go tool cover -func=coverage.out | tail -1
  coverage: '/total:\s+\(statements\)\s+(\d+\.\d+)%/'
  artifacts:
    paths:
      - coverage.out
    expire_in: 1 week

# ------------------------------------------------------------------ test integration
test:integration:
  stage: test
  services:
    - name: pgvector/pgvector:pg16
      alias: postgres
  variables:
    POSTGRES_USER: app
    POSTGRES_PASSWORD: app_password
    POSTGRES_DB: document_hub
    TEST_POSTGRES_DSN: "host=postgres port=5432 user=app password=app_password dbname=document_hub sslmode=disable TimeZone=UTC"
  script:
    - go test -tags=integration -race ./internal/module/user/repository/... ./test/integration/...

# ------------------------------------------------------------------ migrate check (up rồi down)
migrate:check:
  stage: test
  services:
    - name: pgvector/pgvector:pg16
      alias: postgres
  variables:
    POSTGRES_USER: app
    POSTGRES_PASSWORD: app_password
    POSTGRES_DB: document_hub
    # config.dev.yaml đã trỏ host "postgres", user "app".
    APP_POSTGRES_PASSWORD: app_password
    APP_JWT_SECRET: ci-secret
  script:
    - go build -o bin/migrate ./cmd/migrate
    - ./bin/migrate -config configs/config.dev.yaml up
    - ./bin/migrate -config configs/config.dev.yaml down 1

# ------------------------------------------------------------------ build
build:
  stage: build
  script:
    - mkdir -p bin
    - go build -ldflags "-s -w -X main.version=$CI_COMMIT_SHORT_SHA" -o bin/api ./cmd/api
    - go build -o bin/migrate ./cmd/migrate
    - go build -o bin/seed ./cmd/seed
  artifacts:
    paths:
      - bin/
    expire_in: 1 week

# ------------------------------------------------------------------ docker
docker:build:
  stage: docker
  image: docker:27
  services:
    - docker:27-dind
  before_script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
  script:
    - docker build -f deployments/docker/Dockerfile --build-arg VERSION=$CI_COMMIT_SHORT_SHA -t "$DOCKER_IMAGE" .
    - docker push "$DOCKER_IMAGE"
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_TAG'
