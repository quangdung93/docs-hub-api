# Mẫu biến môi trường cho stack EC2 (deployments/ec2/docker-compose.yml).
# Trên EC2: cp .env.ec2.example .env.ec2 rồi điền giá trị thật. KHÔNG commit .env.ec2.
#
# Sinh nhanh một secret ngẫu nhiên: openssl rand -base64 32

# --- Hạ tầng chạy cùng máy (đặt gì cũng được, nhưng phải đủ mạnh) ---
APP_POSTGRES_PASSWORD=
APP_REDIS_PASSWORD=
APP_RABBITMQ_PASSWORD=
# MinIO yêu cầu tối thiểu 8 ký tự.
APP_MINIO_SECRET_KEY=

# --- Ứng dụng ---
APP_JWT_SECRET=

# --- RAGFlow (bắt buộc để đăng ký search/conversation routes) ---
APP_RAGFLOW_ENABLED=true
APP_RAGFLOW_BASE_URL=https://ragflow.io.vn
APP_RAGFLOW_API_KEY=
APP_RAGFLOW_DATASET_PREFIX=docs_hub_ec2

# --- MCP (cổng tích hợp read-only; yêu cầu RAGFlow được bật) ---
APP_MCP_ENABLED=true
APP_MCP_REQUESTS_PER_WINDOW=30
APP_MCP_WINDOW=1m
APP_MCP_MAX_SOURCE_LINES=200
APP_MCP_MAX_EXCERPT_CHARS=65536
